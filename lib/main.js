if (typeof define !== 'function') {
  define = require('amdefine')(module);
}
define(function(require, exports, module) {
  var file, utils;
  file = require('./file');
  utils = require('./utils');
  return exports;
});